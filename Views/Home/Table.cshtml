@model List<Student>
@{
    ViewData["Title"] = "Table";
}
<div class="text-center">
    <h2>Таблица:</h2>
    <table class="table-bordered">
        <thead>
            <tr>
                <td>Имя</td>
                <td>Дата Решения</td>
                <td>Результат</td>
            </tr>
        </thead>
        <tbody>
            @foreach (Student s in Model)
            {
                <tr>
                    <td>@s.Name</td>
                    <td>@s.Date</td>
                    <td>@s.Result</td>
                </tr>
            }
        </tbody>
    </table>
</div>